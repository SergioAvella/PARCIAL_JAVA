<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>PROYECTO_INMOBILIARIA</display-name>
    <description>Sistema inmobiliario UTS - Jakarta EE (Servlets + JSP + JDBC + MySQL)</description>

    <!-- Página principal de inicio -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>

    <!-- Configuración de la sesión de usuario -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
        </cookie-config>
    </session-config>

    <!-- La autenticación y el control de acceso por rol se gestionan mediante la
         anotación @WebFilter en 'filtros.FiltroAutenticacion' (no duplicar aquí).
         El catálogo público /propiedades y las páginas publicas (index, login,
         registro, detalle-propiedad) NO están filtrados. -->

    <!-- Manejo de Páginas de Error -->
    <error-page>
        <error-code>403</error-code>
        <location>/acceso-denegado.jsp</location>
    </error-page>

    <error-page>
        <error-code>404</error-code>
        <location>/login.jsp</location>
    </error-page>

    <!-- Codificación global UTF-8 para evitar problemas de tildes y caracteres -->
    <request-character-encoding>UTF-8</request-character-encoding>
    <response-character-encoding>UTF-8</response-character-encoding>

</web-app>