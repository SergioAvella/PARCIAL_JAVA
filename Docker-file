FROM tomcat:10.0-jdk17-openjdk-slim

# Eliminar la app por defecto de Tomcat
RUN rm -rf /usr/local/tomcat/webapps/*

# Copiar tu archivo WAR generado
COPY tu-proyecto.war /usr/local/tomcat/webapps/ROOT.war

EXPOSE 8080
CMD ["catalina.sh", "run"]